[package]
name = "switchback-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Offline metrics CLI for Switchback fixtures and local extracts"

[dependencies]
serde_json = "1.0.145"
switchback-ingest = { path = "../ingest" }
switchback-matcher = { path = "../matcher" }
switchback-osm = { path = "../osm" }
switchback-trailpack = { path = "../trailpack" }

[lints]
workspace = true
