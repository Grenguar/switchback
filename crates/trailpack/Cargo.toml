[package]
name = "switchback-trailpack"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Versioned, attributable static regional trail data for Switchback"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"

[lints]
workspace = true
