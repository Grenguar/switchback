# Browser-visible, restricted Amazon Location Maps V2 key.
# Restrict it to geo-maps:* + arn:aws:geo-maps:<region>::provider/default
# and the deployed Netlify site referrer before setting it here or in Netlify.
VITE_AWS_LOCATION_API_KEY=
VITE_AWS_LOCATION_REGION=eu-west-1
